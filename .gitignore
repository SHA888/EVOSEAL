# ── Python ────────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
*.so
*.pyd
.Python
.python-version

# ── Build & packaging ─────────────────────────────────────────────────────────
build/
dist/
develop-eggs/
downloads/
eggs/
.eggs/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
*.egg
.installed.cfg
MANIFEST
*.manifest
*.spec
pip-log.txt
pip-delete-this-directory.txt
__pypackages__/
.pybuilder/

# Allow scripts/lib (would otherwise be shadowed by lib64/ glob)
!/scripts/lib/

# ── Virtual environments ──────────────────────────────────────────────────────
.venv/
.venv*
venv/
env/
ENV/
env.bak/
venv.bak/

# ── Testing & coverage ────────────────────────────────────────────────────────
.tox/
.nox/
.coverage
.coverage.*
coverage.xml
*.cover
htmlcov/
nosetests.xml
.pytest_cache/
.pytest-xdist/
.hypothesis/
.cache
cover/
coverage/

# ── Type checkers ─────────────────────────────────────────────────────────────
.mypy_cache/
.dmypy.json
dmypy.json
.pyre/
.pytype/
cython_debug/

# ── Jupyter ───────────────────────────────────────────────────────────────────
.ipynb_checkpoints
*/.ipynb_checkpoints/*

# ── Docs build ────────────────────────────────────────────────────────────────
docs/_build/
/site
.mkdocs-touch

# ── Logs & databases ──────────────────────────────────────────────────────────
*.log
logs
dev-debug.log
*.sqlite
*.sqlite3
*.sqlite3-*
*.db
*.db-journal
*.sqlite3-journal

# ── Environment & secrets ─────────────────────────────────────────────────────
# .env is the live config file (API keys, EVOSEAL_* settings) — never commit
.env
.env.*
.env.local
.env.development.local
.env.test.local
.env.production.local
secrets.*
secrets.py
config/secrets.py
*.pem
*.key
*.crt
*.cer
*.p12
*.pfx
*.p7b
*.p7c
*.p7s
*.p8
*.p10

# ── IDE & editors ─────────────────────────────────────────────────────────────
.idea/
*.iml
*.ipr
*.iws
.idea_modules/
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
!.vscode/*.code-snippets
.history/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
*.swp
*.swo
.ropeproject/
.spyderproject
.spyproject
.project
.pydevproject
.settings/
*.code-workspace
.windsurf/
.semgrep-venv/

# ── OS ────────────────────────────────────────────────────────────────────────
.DS_Store
.AppleDouble
.LSOverride
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/
*~
.fuse_hidden*
.directory
.Trash-*
.nfs*
*.cab
*.msi
*.msix
*.msm
*.msp
*.lnk

# ── Misc tooling ─────────────────────────────────────────────────────────────
.pdm.toml
.bandit
.safety-policy.yml
.trufflehog/
node_modules/
.taskmaster/tasks/tasks.json
.taskmaster/reports/

# ── Metrics & release artifacts ───────────────────────────────────────────────
/metrics/evolution_*.json
!/metrics/evolution_v*.json
/metrics/release_notes_*.md
!/releases/*/RELEASE_NOTES.md
/archived_metrics/

# ── Evolution & runtime data ──────────────────────────────────────────────────
data/continuous_evolution/
data/evolution_data/
data/training_data/
data/model_versions/
data/checkpoints/
!data/examples/
!data/templates/
!data/sample_*

# ── Project-specific ──────────────────────────────────────────────────────────
# Runtime directories (Docker volumes; generated at runtime)
checkpoints/
out/
# EVOSEAL runtime state (pipeline state, rollback targets, logs)
.evoseal/

# Submodule directories (tracked as gitlinks — listed here for tooling clarity)
dgm/
openevolve/
SEAL/

# Local tooling state
.claude-plugin/
hooks/
.claude/
!.claude/settings.json
.harness-mem/state

# Generated artifacts that must not be committed
:memory:
test_results_phase2_simplified.json
requirements/
tasks/
